# MUDDYWATER — SC-004 Master Scenario Events List

**Scenario ID:** SC-004

**Audience for this file:** infra build team — the engineers who provision VMs, configure networking, install services, and populate data themes.

---

## 1. Event List

| ID | Time | Activity | Inject | Description | Additional Comments |
|---|---|---|---|---|---|
| 1 | 0m | Initial Access | `A01_Phishing_Email_Delivery` | A government-themed spearphishing email is delivered to `k.johnson@gov.local` (Finance, WS-1) via MAIL-01, carrying a malicious WinRAR archive attachment. | Attacker sends the email from a spoofed sender designed to look like an internal government contact, routing it through MAIL-01 (172.16.4.10) straight into k.johnson's inbox to set up the WinRAR lure. |
| 2 | 45m | Initial Access / Execution | `A02_Archive_Opened_CVE_Exploit` | k.johnson opens the malicious WinRAR archive on WS-1, triggering CVE-2025-8088. | Attacker abuses the WinRAR 7.10 path-traversal flaw on WS-1 (192.168.210.101): the archive's path-traversal entry writes implant.ps1 outside the extraction folder, planting it directly into the user's Startup folder for execution on the next login. |
| 3 | 50m | Execution | `A03_PowerShell_Implant_Foothold` | The dropped payload executes, establishing a PowerShell implant as the first foothold on WS-1. | Attacker's implant comes online on WS-1 and opens an outbound command-and-control channel to NS1.updates-gov.net (172.16.50.10) via DNS beacons, giving the attacker hands-on-keyboard access to the host. |
| 4 | 55m | Persistence | `A04_Persistence_ScheduledTask_RunKey` | Persistence is established on WS-1 through a Scheduled Task and an HKCU Run registry key. | Attacker locks in persistence by registering a Scheduled Task (WindowsUpdateCheck, ONLOGON trigger) and an HKCU Run key (SecurityHealthUpdater) on WS-1, so the implant restarts automatically every time k.johnson logs back in. |
| 5 | 1h 10m | Credential Access | `A05_Credential_Harvest` | The implant accesses local credential material staged on WS-1 in `passwords.csv`. | Attacker pulls plaintext credentials out of `passwords.csv` on WS-1 (Desktop/Documents/Downloads), harvesting e.brown and l.wilson accounts needed to move into the finance and supplier workstations next. |
| 6 | 1h 30m | Lateral Movement | `A06_Lateral_Movement_WS2_WS3` | The attacker authenticates via SMB to WS-2 (`e.brown`, Finance Supervision) and WS-3 (`l.wilson`, Supplier Coordination) using the harvested credentials. | Attacker pivots from WS-1 (192.168.210.101) into WS-2 (192.168.210.102) and WS-3 (192.168.210.103) over SMB (TCP/445), dropping WinSecUpdate.bat into Startup folders on both hosts. WS-3 remains a lateral-movement pivot only — no document collection occurs there. |
| 7 | 2h 0m | Collection | `A07_Collection_Workstations` | Sensitive documents are collected from WS-1 and WS-2 — finance budgets, payment records, and supervisor approvals. WS-3 carries no collection artefacts; it remains a lateral-movement pivot only. | Attacker sweeps Finance content on WS-1 (locally) and WS-2 (via SMB C$ share) filtering for budget, finance, payment, invoice, salary, contract and approval documents in .docx/.xlsx/.pdf format, staging them locally on WS-1 under C:\ProgramData\upd_logs\docs\. |
| 8 | 2h 30m | Collection | `A08_Collection_FileServer` | The central file server (`FILES-01`, `\\FileServer\public_folder`) is targeted as the primary shared-storage collection source. | Attacker reaches into FILES-01 (10.10.10.30) via the already-mapped public_folder share and pulls all .docx/.xlsx/.pdf files, treating it as the shared staging point every domain user can read. Macro injection into .docx/.xlsx is attempted as a secondary propagation step. |
| 9 | 3h 0m | Collection / Staging | `A09_Archive_Staging` | Collected documents — images, Word files, and spreadsheets — are packaged into an encrypted archive on WS-1. | Attacker bundles everything collected from WS-1, WS-2 and FILES-01 together with sysinfo and credential files into a single 7-Zip encrypted archive (password: Upd@te2024!) on WS-1 under C:\ProgramData\, ready for exfiltration. |
| 10 | 3h 30m | C2 / Exfiltration | `A10_DNS_Tunnel_C2_Exfil` | The implant communicates with attacker-controlled DNS infrastructure (`NS1.updates-gov.net`, WAN) via encoded DNS queries, exfiltrating the staged archive through this channel. | Attacker's implant hex-encodes the archive, splits it into 30-byte chunks and sends each as a DNS query label to NS1.updates-gov.net (172.16.50.10), encoding roughly 181 chunks over ~20 seconds so the traffic blends into the everyday resolver noise coming out of the USER LAN. |
